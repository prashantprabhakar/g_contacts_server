// @TODO: Use .env files for different config
const config = {
    default_port: 4000,
    logging: {
        level: 'info',
        logDir: 'logs',
        logFile: 'dev_g_contacts'
    }
}

module.exports = config